import{describe,expect,it}from'vitest';import seed from'../../../../../optimus-be/internal/seed/seed.go?raw';import main from'../../../main.ts?raw';import routes from'../../../router/static-routes.ts?raw'
const views=import.meta.glob('../**/*.vue',{query:'?raw',import:'default',eager:true})as Record<string,string>
describe('P6 navigation and permission audit',()=>{it.each([
  ['../projects/List.vue','delivery:project:write'],['../projects/List.vue','delivery:project:delete'],['../projects/Detail.vue','delivery:project:write'],['../projects/components/PipelineForm.vue','delivery:pipeline:write'],['../runs/Detail.vue','delivery:run:cancel'],['../runs/Detail.vue','delivery:run:create'],['../approvals/List.vue','delivery:approval:decide'],
])('%s carries exact concrete control permission %s',(file,permission)=>expect(views[file]).toContain(`v-permission="'${permission}'"`))
it('gates independent read surfaces exactly',()=>{expect(views['../projects/Detail.vue']).toContain("permission.has('delivery:run:read')");expect(views['../projects/Detail.vue']).toContain("permission.has('delivery:run:create')");expect(views['../approvals/List.vue']).toContain("permission.has('delivery:approval:read')")})
it('seeded menu components resolve with Linux casing and run detail is a protected child',()=>{for(const component of['delivery/projects/List','delivery/approvals/List']){expect(seed).toContain(`Component: "${component}"`);expect(views[`../${component.replace('delivery/','')}.vue`]).toBeTypeOf('string')}expect(routes).toContain("path: 'delivery/runs/:id(\\\\d+)'");expect(routes).toContain("permission: 'delivery:run:read'")})
it('logout resets active streams and no unsafe run editor exists',()=>{expect(main).toContain('useDeliveryStore().reset()');const source=Object.values(views).join('\n').toLowerCase();expect(source).not.toContain('raw-log');expect(source).not.toContain('values_yaml');expect(source).not.toContain('helm flags')})})
