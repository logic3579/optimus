import { mount } from '@vue/test-utils';import{describe,it,expect}from'vitest';import Form from'../Form.vue'
describe('dashboard form',()=>{it('validates aggregate editor and moves panels with approved types/units/widths',()=>{const w=mount(Form,{props:{datasources:[{id:1,name:'p'}]},global:{stubs:{'a-form':{template:'<form><slot/></form>'},'a-input':true,'a-select':true,'a-button':true}}}),vm=w.vm as unknown as{addPanel():void;move(index:number,delta:number):void;form:{name:string;refresh_interval_s:number;time_range:string;panels:{title:string;panel_type:string;unit:string;width:number}[]};valid:boolean};vm.form.name='d';vm.addPanel();vm.form.panels[0]!.title='CPU';expect(vm.valid).toBe(true);expect(['time_series','stat','table']).toContain(vm.form.panels[0]!.panel_type);expect([6,12]).toContain(vm.form.panels[0]!.width);vm.addPanel();vm.move(1,-1);expect(vm.form.panels[0]!.title).toBe('Panel')})})
