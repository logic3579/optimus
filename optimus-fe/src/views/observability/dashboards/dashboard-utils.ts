import type { Panel, Query } from '@/types/observability'
export function groupPanelQueries(panels: Panel[]): Map<number, Query[]> { const groups = new Map<number, Query[]>(); for (const panel of [...panels].sort((a,b)=>a.sort_order-b.sort_order)) { const items=groups.get(panel.datasource_id)??[];items.push({ref_id:`panel-${panel.id}`,promql:panel.promql});groups.set(panel.datasource_id,items) } return groups }
export function createRefreshController(run:(signal:AbortSignal)=>Promise<void>,hidden:()=>boolean,make=()=>new AbortController()){let active:ReturnType<typeof make>|undefined;let isHidden=false;return{async refresh(){if(isHidden||hidden())return;active?.abort();active=make();await run(active.signal)},setHidden(value:boolean){isHidden=value;if(value){active?.abort();active=undefined}},dispose(){active?.abort();active=undefined}}}
