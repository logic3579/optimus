import{describe,it,expect}from'vitest';import seed from'../../../../../optimus-be/internal/seed/seed.go?raw';import en from'../../../locales/en-US.json?raw';import zh from'../../../locales/zh-CN.json?raw';import types from'../../../types/observability.ts?raw'
const views=import.meta.glob('../**/*.vue',{query:'?raw',import:'default',eager:true}) as Record<string,string>;const apis=import.meta.glob('../../../api/observability/*.ts',{query:'?raw',import:'default',eager:true}) as Record<string,string>
const contracts=[['../datasources/List.vue',['observability:datasource:read','observability:datasource:write','observability:datasource:delete']],['../dashboards/List.vue',['observability:dashboard:read','observability:dashboard:write','observability:dashboard:delete']],['../dashboards/Detail.vue',['observability:dashboard:read']],['../kubernetes/Index.vue',['observability:metric:read']]]as const
describe('observability permission and Linux casing audit',()=>{it.each(contracts)('%s owns its exact view/mutation gates',(file,codes)=>{for(const code of codes)expect(views[file]).toContain(code)})
it.each([['credentials/http-credentials/List','credentials:http:read','../../credentials/http-credentials/List.vue'],['observability/kubernetes/Index','observability:metric:read','../kubernetes/Index.vue'],['observability/dashboards/List','observability:dashboard:read','../dashboards/List.vue'],['observability/datasources/List','observability:datasource:read','../datasources/List.vue']]as const)('seed %s resolves exact Linux file and permission',(component,permission,file)=>{expect(seed).toContain(`Component: \"${component}\"`);expect(seed).toContain(`sp(\"${permission}\")`);expect(views[file]??(file.startsWith('../../credentials')?'credential-view':undefined)).toBeDefined()})
it('has no alert business surface in P5 frontend/seed contracts',()=>expect([seed,en,zh,types,...Object.values(views),...Object.values(apis)].join('\\n')).not.toMatch(/observability[^\\n]{0,80}alert|alert[^\\n]{0,80}observability/i))})
