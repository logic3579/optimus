import type{BuiltinDashboard,Query}from'@/types/observability'
const labelSafe=/^[A-Za-z0-9_.:-]*$/;const placeholder=/\$\{([A-Za-z_][A-Za-z0-9_]*)\}/g
const escapeProm=(value:string)=>value.replaceAll('\\','\\\\').replaceAll('"','\\"').replaceAll('\n','\\n')
const escapeRegex=(value:string)=>value.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')
export function renderBuiltinQueries(definition:BuiltinDashboard,values:Record<string,string>):Query[]{const allowed=new Map(definition.variables.map(v=>[v.name,v]));for(const key of Object.keys(values))if(!allowed.has(key))throw new Error(`unknown variable ${key}`);for(const variable of definition.variables){const value=values[variable.name]??'';if(variable.required&&!value)throw new Error(`variable ${variable.name} is required`);if(!labelSafe.test(value))throw new Error(`variable ${variable.name} is not label-safe`)}return definition.panels.map(panel=>{const promql=panel.promql.replace(placeholder,(_match:string,name:string)=>{const variable=allowed.get(name);if(!variable)throw new Error(`unresolved variable ${name}`);const value=values[name]??'';return escapeProm(variable.regex?escapeRegex(value):value)});if(placeholder.test(promql))throw new Error('unresolved variable');placeholder.lastIndex=0;return{ref_id:panel.ref_id,promql}})}
