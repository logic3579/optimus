run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gocritic
    - revive
    - gofmt
    - goimports

issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
